# Re-run the outcome on multiple measures: within-cohort citation
# percentile and awards are the hard rulers; the CD index gets a single
# vote only, since it is inflation-contaminated.

outcome_tier_analysis <- function(hazard_table) {
  stop("not implemented")
}
