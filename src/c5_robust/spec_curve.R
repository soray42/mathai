# Specification-curve analysis over the reasonable modelling choices.
# The verdict rests on sign consistency across the multiverse, not on a
# single specification's p-value.

specification_curve <- function(data, spec_grid) {
  stop("not implemented")
}
