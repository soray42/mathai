# Stratify by AI subdomain to guard against Simpson's paradox, with
# partial pooling so sparse subdomain cells borrow strength.

stratify_by_subdomain <- function(hazard_table) {
  stop("not implemented")
}
