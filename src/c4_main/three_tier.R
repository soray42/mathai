# Three-tier comparison guarding against the citation-inflation artefact.
#
# raw  ->  inflation-deflated  ->  censoring-corrected. Petersen et al.
# show a disruption decline can be a pure inflation artefact, so the gap
# between the raw and corrected trend is reported openly rather than
# hidden.

run_three_tier <- function(hazard_table, inflation_index) {
  stop("not implemented")
}
