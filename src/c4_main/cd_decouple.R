# Estimate the hazard trend and the CD trend in parallel and check whether
# they decouple after deflation. This is a parallel-trends comparison, not
# a placebo covariate: CD is inflation-contaminated, so it is read as an
# independent series rather than a control.

decouple_hazard_vs_cd <- function(hazard_trend, cd_trend) {
  stop("not implemented")
}
