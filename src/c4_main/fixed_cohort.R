# Deflation-free main proof.
#
# Cohort fixed effects absorb the drift in citation norms across periods,
# so the cohort trend is identified without any deflation model. The
# three-tier deflation is then only a robustness layer on top of this.

fit_fixed_cohort <- function(hazard_table) {
  stop("not implemented")
}
