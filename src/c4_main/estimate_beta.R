# Estimate the cohort trend in adoption hazard.
#
# beta is the per-cohort shift; the pre-registered falsifiable hypothesis
# is beta <= 0 (newer cohorts adopt older mathematics no faster).

fit_cohort_trend <- function(hazard_table) {
  stop("not implemented")
}
